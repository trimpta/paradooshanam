# In Page Layout

The **In Page** is the page for a graph's records, where the user can view the current records and add new records.

## Header and Bottom Bar
- The duplicate header currently appearing at the top and bottom due to glitches must be removed from the top.
- Only the bottom bar should remain.
- The bottom bar contains: IN/OUT toggle, Menu button.
- The app name should NOT be displayed in the In Page bottom bar.

## Cards List
- Existing records list remains.
- Add one additional card at the **top of the cards list** for Searching and Filtering.


*Technical Implementation: See `App.tsx` in [Architecture](../tech/02-architecture.md)*
